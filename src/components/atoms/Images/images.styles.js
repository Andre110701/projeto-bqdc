import styled, { css } from "styled-components"

export const Images = styled.img`
${({theme})=>css`
width: 100%;
max-height: 80vh;
border-radius: 16px;
`}`

export const Logo = styled.img`
${({theme})=>css`
`}`